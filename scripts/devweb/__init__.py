"""Development stand-ins for the swf-monitor runtime.

A stub base template and swf_fmt template tags so the packaged app's
pages render outside the platform. Used only by scripts/webdev.py;
never installed in a hosted deployment, where monitor_app provides the
real base template and tags.
"""
